name: Integration Tests

on:
  pull_request:
    branches: [ "main" ]
  push:

jobs:
  call-workflow:
    uses: ./.github/workflows/test-plugin.yml
    with:
        triton-shared-ref: ${{ github.ref }}
